// Tailwind v4 uses CSS-first configuration.
// All design tokens, the `dark:` variant, and the typography plugin are
// configured in `src/app/globals.css` via `@theme`, `@custom-variant`, and
// `@plugin` directives.
//
// This file is intentionally left empty — Tailwind v4 will not auto-load it.
// If you ever need to reach for the legacy JS config, reference it from CSS
// with `@config "../../tailwind.config.ts";` in globals.css.
export {};
